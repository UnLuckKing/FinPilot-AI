{
  "name": "finpilot-universal-analyzer",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.9"
  },
  "scripts": {
    "start": "node server/index.mjs",
    "dev": "node --watch server/index.mjs",
    "test": "node --test tests/*.test.mjs",
    "check:pine": "node scripts/check-pine.mjs",
    "check": "npm test && npm run check:pine",
    "demo": "node scripts/send-demo.mjs"
  }
}
